# 部署规范

## 架构

```text
GitHub -> Jenkins -> 腾讯云 CCR -> Docker Compose -> Nginx
```

## 1. 镜像不可变

每次构建生成唯一、不可变的镜像版本，保证部署产物可追溯。

镜像标签使用 Git Commit SHA，不复用已经发布的标签。

## 2. 蓝绿部署

生产发布采用蓝绿部署，降低停机风险并保留快速回滚能力。

## 3. Health Check 与回滚

每次部署必须执行 Health Check。新环境 Readiness 未通过时，不切换业务流量，并立即执行 Rollback。
