# 编码规范

## 1. 单一职责

每个模块、类和函数只承担一个清晰职责，便于理解、测试和维护。

例如，一个 Service 只负责一个业务领域，不同时处理无关业务。

## 2. 禁止魔法值

具有业务含义的数字或字符串必须使用命名常量或配置，避免在代码中直接出现含义不明的值。

```python
TIMEOUT_SECONDS = 30
```

## 3. Review 门槛

所有 Story 或 PR 必须通过 AI Review，以统一代码质量。Review 规则和评分要求见 `CodeReview规范.md`。
